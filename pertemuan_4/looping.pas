program luplup;
uses crt;
label a, b, c, d, e;
var
    i, j, n: integer;
begin
    clrscr;
    // for i := 1 to 10 do
    // begin
    //     writeln('Maajid Ganteng');
    // end;

    // for i := 10 downto 1 do
    //   writeln(i);

    // write('input n: ');
    // readln(n);
    // for i := 1 to n do
    // begin
    //   for j := 1 to i do
    //   begin
    //     write('* ');
    //   end;
    //   writeln;
    // end;

    // write('input n: ');
    // readln(n);
    // for i := 1 to n do
    // begin
    //   for j := 1 to i do
    //   begin
    //     if (i mod 2 = 0) then
    //       write(i, ' ')
    //     else
    //       write('* ');
    //   end;
    //   writeln;
    // end;

    // segigigit bintang bintang
    // write('input n: ');
    // readln(n);
    // for i:= 1 to n do
    // begin
    //   for j := 1 to n - i do
    //     write(' ');
    //   for j := 1 to 2*i-1 do
    //     write('*');
    //   writeln;
    // end;

    // Statement while
    // i := 1;
    // while (i <= 5) do
    // begin
    //   writeln('IKLC !');
    //   inc(i, 1);
    // end;

    // // Statement repeat until
    // i := 0;
    // repeat
    //   i := i + 1;
    //   writeln(i);
    // until i = 5;

end.
